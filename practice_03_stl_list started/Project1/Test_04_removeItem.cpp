#include "Testing.h"

void testRemoveItem()
{
	//cout << "TEST: removeItem\n\n";
	//{
	//	for (size_t i = 0; i < VECTOR_SIZE; ++i)
	//	{
	//		AnyList list1;
	//		for (size_t j = 0; j < VECTOR[i].size(); ++j)
	//			list1.insertBack(VECTOR[i][j]);

	//		cout << "    List is: ";
	//		ostream_iterator<int> screen(cout, " ");
	//		copy(VECTOR[i].crbegin(), VECTOR[i].crend(), screen);
	//		cout << "\nRemove " << (VECTOR[i][(VECTOR[i].size() - 1)]);
	//		list1.removeItem(VECTOR[i][(VECTOR[i].size() - 1)]);
	//		cout << " => Your output: ";
	//		cout << list1;

	//		cout << "\nRemove " << (VECTOR[i][(VECTOR[i].size() - 1) / 2]);
	//		list1.removeItem(VECTOR[i][(VECTOR[i].size() - 1) / 2]);
	//		cout << " => Your output: ";
	//		cout << list1;

	//		cout << "\nRemove " << ((VECTOR[i][0]));
	//		list1.removeItem(VECTOR[i][0]);
	//		cout << " => Your output: ";
	//		cout << list1;
	//		cout << endl << endl;
	//	}
	//}
	//cout << "\n--------------------------------------\n";
}
