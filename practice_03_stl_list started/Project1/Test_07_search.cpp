#include "Testing.h"

void testSearch()
{
	//cout << "TEST: search\n\n";
	//{
	//	for (size_t i = 0; i < VECTOR_SIZE; ++i)
	//	{
	//		AnyList list1;
	//		for (size_t j = 0; j < VECTOR[i].size(); ++j)
	//			list1.insertBack(VECTOR[i][j]);

	//		cout << "List is: ";
	//		cout << list1;

	//		cout << "\n   Search " << VECTOR[i][0] << " => ";
	//		cout << ((list1.search(VECTOR[i][0])) ? "Found." : "Not found.");
	//		cout << "\n   Search " << VECTOR[i][1] << " => ";
	//		cout << ((list1.search(VECTOR[i][1])) ? "Found." : "Not found.");
	//		cout << "\n   Search " << (VECTOR[i].size() - 1) << " => ";
	//		cout << ((list1.search(static_cast<int>(VECTOR[i].size()) - 1)) ? "Found." : "Not found.");
	//		cout << "\n   Search 1000 => ";
	//		cout << ((list1.search(100)) ? "Found." : "Not found.");

	//		cout << endl << endl;
	//	}
	//}
	//cout << "\n--------------------------------------\n";
}