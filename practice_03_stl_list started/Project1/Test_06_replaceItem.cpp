#include "Testing.h"

void testReplaceItem() 
{
	//cout << "TEST: replaceItem\n\n";
	//{
	//	vector<vector<int>> vReplace{
	//		{ 50 },{ 50 },{ 10 },
	//		{ 300, 500, 400, 900, 200 },{ 200 },{ 999 },
	//		{ 30, 50, 40, 90, 20, 10 },{ 40 },{ 100 },
	//		{ 60, 40, 20, 70 },{ 60 },{ 333 },
	//	};

	//	for (size_t i = 0; i < vReplace.size(); i += 3)
	//	{
	//		AnyList list1;
	//		// create vector
	//		vector<int> v = vReplace[i];
	//		// populate list
	//		for (size_t j = 0; j < vReplace[i].size(); ++j)
	//			list1.insertBack(vReplace[i][j]);

	//		// output lists			
	//		cout << "   List 1 is: ";
	//		ostream_iterator<int> screen(cout, " ");
	//		copy(v.cbegin(), v.cend(), screen);

	//		// replace item in vector
	//		cout << " => Replace " << vReplace[i + 1][0] << " with " << vReplace[i + 2][0];
	//		replace(v.begin(), v.end(), vReplace[i + 1][0], vReplace[i + 2][0]);

	//		// replace item in list
	//		list1.replaceItem(vReplace[i + 1][0], vReplace[i + 2][0]);

	//		// print out results
	//		cout << "\nAfter replacement is done...";
	//		cout << "\n    Expected: ";
	//		copy(v.cbegin(), v.cend(), screen);
	//		cout << "\n Your output: ";
	//		cout << list1;
	//		cout << endl << endl;
	//	};
	//}
	//cout << "\n--------------------------------------\n";

}
